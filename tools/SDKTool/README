简介：
    SDKTool是与GAME_AI_SDK（http://git.code.oa.com/turinglab_opensource/game_ai_sdk.git）配套使用的工具，用于配置task任务，UI等

依赖：
    opencv-contrib-python
    tensorflow(tensorflow-gpu)==1.3.0
    pyqt5
    qtpy
    labelme
    scikit-image
    tqdm

    若仅用于配置文件的配置，安装tensorflow即可，若要使用像素语义的样本标注功能，那么cpu的tensorflow会较慢，建议安装tensorflow-gpu==1.3.0
    并自行安装cuda-8.0

使用：
    cd <path of SDKTool>
    virtualenv -p /usr/bin/python3 env         //创建虚拟环境（windows的命令请自行查询）
    source env/bin/activate
    pip install -r requirements.txt
    python main.py

